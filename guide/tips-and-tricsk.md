**Tips And Tricks**

# Launching Emacs with a particular profile

**Using the --init-directory flag**
This is the most straightforward method for modern Emacs versions. It allows you to specify a custom directory containing your Emacs configuration files (e.g., init.el, elpa/, etc.).
```sh
emacs --init-directory $custom-directory$
```
